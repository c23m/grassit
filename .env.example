# 复制为 .env 后使用（.env 已被 .gitignore 忽略）
MYSQL_ROOT_PASSWORD=change-me
MYSQL_USER=grassit
MYSQL_PASSWORD=change-me
MYSQL_DATABASE=grassit

# 后端签发 access token 用的密钥（compose 会注入给 backend 容器）
JWT_SECRET=change-me

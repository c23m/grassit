# 复制为 .env 后按需修改（.env 不入版本库）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=grassit
DB_PASSWORD=change-me
DB_NAME=grassit

# 静态资源 / 上传文件的存储根目录（必须存在，否则后端启动时报错）
PUBLIC_DIR=./public

# 签发 access token 用的密钥，必须自行生成，不要用默认值
# 生成方式：python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=change-me

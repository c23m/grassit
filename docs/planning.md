# Grassit 产品需求文档

## 一、目标

为个人博客 + Wiki 提供一个自托管的内容系统，支持文章发布、用户认证、文件存储。

## 二、不做什么

- 不做多租户（只有单一站点，不服务多个独立博客）
- 不做富文本编辑器（用 Markdown）

## 三、用户系统

### 3.1 注册

**必填：**

- 用户名 `username`
- 昵称 `nickname`
- 密码 `password`

**可选：**

- 邮箱 `email`

### 3.2 用户字段

| 字段             | 约束                                                                   | 可变      |
| ---------------- | ---------------------------------------------------------------------- | --------- |
| `username`       | 唯一、必需，≤30 字符，只允许 `a-z A-Z 0-9 - _`，不能以 `-` 或 `_` 开头 | ❌ 不可变 |
| `nickname`       | 必需，≤30 字符，不唯一                                                 | ✅ 可变   |
| `created_at`     | 精确到日                                                               | ❌        |
| `last_online_at` | 精确到分                                                               | 自动更新  |
| `is_online`      | 在线 / 离线                                                            | 自动更新  |
| `avatar`         | URL，可空                                                              | ✅ 可变   |
| `email`          | 可空，非空时与账号一对一                                               | ✅ 可变   |
| `github`         | 可空，非空时与账号一对一                                               | ✅ 可变   |
| `status`         | 正常 / 封禁中 / 已注销                                                 | 见 3.4    |

### 3.3 公开信息

以下字段对外可见：

- 用户名
- 昵称
- 创建时间
- 在线状态 / 上次在线时间
- 账号状态

### 3.4 账号状态规则

| 状态   | 可被搜索 | 可见 | 备注                               |
| ------ | -------- | ---- | ---------------------------------- |
| 正常   | ✅       | ✅   |                                    |
| 管理员 | ✅       | ✅   | 可以封禁/解封用户                  |
| 封禁中 | ✅       | ✅   | 显示封禁状态                       |
| 已注销 | ❌       | ❌   | 文章仍存在，作者名显示“已注销用户” |

**封禁：**

- 个人信息可以查看，但是不能登录（显示账号异常）。
- 封禁时设置解封期限，如果超过解封期限，在外部查询此用户时，会自动解封。

**注销后：**

- 用户名不可被新用户注册
- 已发布的文章仍可正常访问

且正常之外的状态，在登陆时会显示账号账号异常，禁止登录。

### 3.5 登录后可修改

- 昵称（可改，不可删）
- 邮箱（可空，非空时一对一）
- GitHub 绑定（同邮箱规则）

## 四、认证

### 4.1 密码登录

- 提供 `username` + `password`
- 成功返回 access token

### 4.2 GitHub 登录

- 前置条件：用户已绑定 GitHub
- 流程：OAuth 授权 → 回调 → 签发 token

### 4.3 邮箱登录

- 用户提供邮箱
- 系统查询绑定的账号
- 存在时发送 6 位数字验证码，有效期10分钟
- 有效期内，用户输入验证码登录

### 4.4 Token 策略

| Token         | 有效期  | 存放              |
| ------------- | ------- | ----------------- |
| access token  | 15 分钟 | 前端 localStorage |
| refresh token | 30 天   | HttpOnly Cookie   |

**请求头：** `Authorization: Bearer <token>`

## 五、文章系统

### 5.1 字段

| 字段         | 约束                                        |
| ------------ | ------------------------------------------- |
| `uuid`       | 唯一，不可变                                |
| `slug`       | 唯一，≤128 字符，只含小写字母和连字符，可变 |
| `title`      | 必需                                        |
| `content`    | 正文                                        |
| `author`     | 关联用户。若用户已注销，显示“已注销用户”    |
| `created_at` | 精确到分                                    |
| `updated_at` | 精确到分                                    |
| `tags`       | 标签集合，可按标签查询                      |
| `word_count` | UTF-8 字符数                                |
| `visibility` | 公开 / 私密，默认公开                       |

### 5.2 可见性规则

| 可见性 | 谁能访问             |
| ------ | -------------------- |
| 公开   | 任何人               |
| 私密   | 仅作者可访问或搜索到 |

### 5.3 删除规则

- **物理删除**，不是软删
- slug 释放，可被其他文章使用
- 删除后通过 uuid 或 slug 均不可访问

## 六、存储

### 6.1 路径约定

存储根目录由后端环境变量 `PUBLIC_DIR` 指定，并以 `/public` 前缀对外提供静态资源（见 `backend/app/main.py`）。

| 环境 | 根路径                       |
| ---- | ---------------------------- |
| 开发 | `./public`（后端工作目录下） |
| 生产 | `/var/lib/grassit/public`    |

### 6.2 目录结构

```
public/
├── static/
├── avatars/
└── attachments/
```

### 6.3 文件命名

对 `slug + 原始文件名` 取 SHA-256，取前 12 位作为存储文件名。

## 七、接口

### 7.1 BaseURL

前端所有请求统一走 `/api` 前缀（同域路径，不使用独立 API 子域）。

| 环境 | 前端请求   | 实际去向                                                                                             |
| ---- | ---------- | ---------------------------------------------------------------------------------------------------- |
| 开发 | `/api/...` | Vite dev server 代理到 `http://localhost:8000`，**剥掉 `/api` 前缀**（见 `frontend/vite.config.js`） |
| 生产 | `/api/...` | Nginx 反代到 `http://127.0.0.1:8000`，同样剥掉 `/api` 前缀                                           |

**路径规则：** 允许尾后斜杠；FastAPI 默认把 `/test/` 以 307 重定向到 `/test`。

### 7.2 接口

路径以实现（`backend/app/routers/`）为准；完整契约由后端导出 `openapi.json`。

| 接口                            | 说明                                                                       |
| ------------------------------- | -------------------------------------------------------------------------- |
| `POST /auth/register`           | 注册                                                                       |
| `POST /auth/login`              | 密码登录，返回 access token                                                |
| `POST /auth/logout`             | 登出，清除 refresh cookie                                                  |
| `POST /auth/refresh`            | 用 refresh token 换新的 access token                                       |
| `GET /users/me`                 | 当前登录用户                                                               |
| `GET /users/{username}`         | 用户公开信息                                                               |
| `DELETE /users/{username}`      | 管理员删除用户                                                             |
| `GET /articles`                 | 文章列表，支持 `author` / `title` / `slug` / `start` / `end` / `tags` 筛选 |
| `GET /articles/{identifier}`    | 文章详情（`identifier` 为 uuid 或 slug）                                   |
| `POST /articles`                | 创建文章                                                                   |
| `DELETE /articles/{identifier}` | 删除文章（仅作者）                                                         |

### 7.3 协议

- 生产环境使用 HTTPS
- 认证头：`Authorization: Bearer <token>`

### 7.4 错误码

| 状态码 | 含义         | 使用场景                                                 |
| ------ | ------------ | -------------------------------------------------------- |
| 400    | 请求不合法   | 业务规则不满足，但请求格式本身没问题（预留）             |
| 401    | 未认证       | 缺少或无效的 token；登录时用户名或密码错误               |
| 403    | 无权限       | 已登录但无权操作，如删除他人的文章                       |
| 404    | 不存在       | 目标用户或文章不存在                                     |
| 409    | 冲突         | 唯一约束冲突：用户名或邮箱已被占用                       |
| 422    | 参数校验失败 | 请求体或查询参数不符合 schema，由 FastAPI 校验层自动返回 |

唯一冲突的 `detail` 需要区分是用户名还是邮箱，便于前端定位到具体输入框。

## 八、版本计划

见 [todo.md](todo.md) 的「版本规划」。

## 九、关键决策

| 决策                       | 理由                                          |
| -------------------------- | --------------------------------------------- |
| 用户名不可变               | 避免 URL 失效，避免混淆                       |
| 文章物理删除               | 简单；个人项目不需要回收站                    |
| 唯一冲突返回 409           | 与 400 / 422 区分开，前端可直接定位到冲突字段 |
| slug 用 SHA-256 派生文件名 | 避免文件名冲突，不暴露原始文件名              |
| 用户名注销后不可复用       | 防止身份混淆                                  |
| 私密文章仅作者可见         | 最简权限模型，不做“分享给特定用户”            |
| access token 15 分钟       | 短期凭证，降低泄露风险                        |
| refresh token 30 天        | 平衡安全与体验                                |

## 十、待定

- 封禁用户能否登录？还是直接拒绝？
- 注销后邮箱是否可复用（和用户名规则一致）？
- GitHub 绑定能否解绑？解绑后如何保证账号可登录？
- 单文件上传大小上限？

---

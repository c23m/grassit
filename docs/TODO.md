# TODO

- [TODO](#todo)
  - [现状](#现状)
  - [计划](#计划)
    - [0.1.1](#011)
    - [0.1.2](#012)
    - [0.1.3](#013)
    - [后续版本](#后续版本)

## 现状

以代码为准（`backend/app/`）。与 [PLANNING.md](PLANNING.md) 的需求相比，以下差距均**尚未实现**：

| 项                   | 需求                          | 现状                                                 |
| -------------------- | ----------------------------- | ---------------------------------------------------- |
| 用户名字段           | `username`                    | 模型中是 `name`（`String(30)`、唯一）                |
| 用户名字长           | ≤30                           | `RegisterRequest` 限制 3~20，`pattern=^[a-zA-Z-_]+$` |
| 账号状态             | 正常 / 封禁 / 已注销          | 无 `status` 字段                                     |
| 在线状态             | `is_online`、`last_online_at` | 无                                                   |
| 头像 / GitHub / uuid | 需要                          | 无                                                   |
| 创建时间精度         | 精确到日                      | `DateTime`（含时间，UTC）                            |
| 文章                 | 表 + 增删改查                 | 无 ORM 模型、无表；接口返回样例数据                  |
| 标签                 | 可按标签查询                  | 无                                                   |
| 认证                 | access / refresh token        | 假 token；`get_current_user` 固定返回样例用户        |
| 静态资源             | 存储 `public/`                | 已挂载到 URL `/public`，目录为 `backend/public/`     |

其他已知问题：

- 前端 `frontend/src/api/` 仍用 `/article`、`/auth/me`、`/user/{username}`，与后端路由（`/articles`、`/users/me`、`/users/{username}`）不一致
- 前端缺 `/login` 路由，`register` 路由未指定组件
- 后端真实路由清单可用 `app.openapi()` 查看

## 计划

### 0.1.1

让网站能够使用.
提交后, 暂时通过slug访问

**前端**:

- [ ] 实现注册页
- [ ] 实现登录页
- [ ] 在导航栏显示当前用户的头像
- [ ] 完成简易文档提交功能

**后端**:

至少实现下面的接口（路径见 [PLANNING.md](PLANNING.md) 的「七、接口」）:

- [ ] `POST /auth/register`
- [ ] `POST /auth/login`
- [ ] `GET /users/me`
- [ ] `POST /articles`

### 0.1.2

部署[**生产环境**](deploy.md).

**前端**:

- [ ] 优化代码

**后端**:

- [ ] 用环境变量替换硬编码的东西(如`/api`等)

### 0.1.3

**前端**:

- [ ] 优化提交页
- [ ] 导航栏添加搜索框
- [ ] 添加仪表盘, 即用户主页
- [ ] 修复测试页（直接打后端真实接口，不引入 mock）

**后端**:

- [ ] 实现所有接口列表里的接口.
- [ ] 导出 `openapi.json`（接口契约），并据此生成接口文档页面

### 后续版本

```
v0.2  用户与文章增强
  ├── 邮箱登录
  ├── GitHub 绑定 + 登录
  ├── 文章删除
  ├── 标签查询
  └── 可见性（公开/私密）

v0.3  用户状态
  ├── 账号状态（封禁/注销）
  ├── 注销后文章保留逻辑
  └── 在线状态记录

v0.4  存储
  ├── 头像上传
  ├── 附件上传
  └── 静态资源服务
```
